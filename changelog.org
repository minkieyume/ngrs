#+TITLE:ChangeLog
* 0.1.2
- 完成了基础类型(number、string、bool、char)的转换。
  Finished convert for base types (ops. number、string、bool、char).
- 重构build.rs代码，添加wrapper.c和wrapper.h方便绑定函数。
  Rebuiled build.rs codes, And added wrapper.c and wrapper.h let it easy to binding functions.

* 0.1.3
- 支持复杂类型pair及scm与pair的互转
  Support diffcult types conversions with pari and scm.
